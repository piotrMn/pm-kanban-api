spring:
  application:
    name: KanbanApi
  jpa:
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
server:
  servlet:
    context-path: /kanban-api
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
